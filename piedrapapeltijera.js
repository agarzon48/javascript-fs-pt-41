const prompt = require("prompt-sync")({sigint:true});

let userNumber = 0;
let computerNumber = 0;

const prompUserSeleccion = {Selection} => {
    const userSelection = prompt
}

while (userNumber) > 0

